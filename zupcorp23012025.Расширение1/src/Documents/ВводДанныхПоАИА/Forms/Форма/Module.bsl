
&НаКлиенте
Процедура Сформировать(Команда)
    Значение = Новый Структура();
	Значение.Вставить("Параметр1", ДатаС);
	Значение.Вставить("Параметр2", ДатаПо);
	ЭтаФорма.Закрыть(Значение);	// Вставить содержимое обработчика.
КонецПроцедуры
